services:
  db:
    image: "postgres:15-alpine"
    environment:
      - POSTGRES_DB=platform
      - POSTGRES_PASSWORD=service
      - POSTGRES_USER=platform_service
    ports:
      - 5432:5432
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data: